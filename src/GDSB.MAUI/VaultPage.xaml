<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:GDSB.MAUI.Views"
             xmlns:behaviors="clr-namespace:GDSB.MAUI.Behaviors"
             xmlns:controls="clr-namespace:GDSB.MAUI.Controls"
             x:Class="GDSB.MAUI.VaultPage"
             x:Name="ThisPage"
             BackgroundColor="{StaticResource SurfaceDark}">

    <!--
      Sem Title de propósito: o nome do cofre já aparece no cabeçalho da página, nos dois
      layouts. Com o Title do Shell também ligado ao VaultName, o layout largo mostrava o
      nome duas vezes no canto superior esquerdo. A barra do Shell fica só com a seta de
      voltar.
    -->

    <!--
      Sobrescreve o botão de voltar padrão do Shell (a seta no canto superior esquerdo): o pop
      relativo padrão ("..") não é confiável aqui - depois de criar/abrir um cofre com
      NavigateToRootAsync (que reseta a pilha até o Unlock), a seta do Shell ainda conseguia cair
      de volta numa tela intermediária já concluída (ex.: criar cofre). Forçar sempre GoHomeAsync
      (volta pro Unlock do zero) cobre tanto a seta quanto o botão físico/gesto de voltar do
      Android, então os dois se comportam igual.
    -->
    <Shell.BackButtonBehavior>
        <BackButtonBehavior Command="{Binding BindingContext.GoHomeCommand, Source={x:Reference ThisPage}}" />
    </Shell.BackButtonBehavior>

    <ContentPage.Resources>
        <DataTemplate x:Key="SecretBoxCardTemplate">
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,10">
                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                        <Border Grid.Column="0" WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 12"
                                BackgroundColor="{StaticResource Tertiary}" StrokeThickness="0">
                            <Label Text="{Binding Initial}" TextColor="{StaticResource White}"
                                   FontFamily="OpenSansSemibold" FontSize="15"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                            <Label Text="{Binding BoxName}" FontFamily="OpenSansSemibold" FontSize="15"
                                   TextColor="{StaticResource TextPrimaryDark}" LineBreakMode="TailTruncation" />
                            <Label Text="{Binding Url}" FontSize="12.5" TextColor="{StaticResource PrimaryDark}"
                                   IsVisible="{Binding HasUrl}" LineBreakMode="TailTruncation"
                                   behaviors:HoverCursor.IsHand="True">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenUrlCommand}"
                                        CommandParameter="{Binding .}" />
                                </Label.GestureRecognizers>
                            </Label>
                        </VerticalStackLayout>
                        <Button Grid.Column="2" Text="&#9733;" FontSize="18" Padding="0"
                                BackgroundColor="Transparent" TextColor="{StaticResource TextMutedDark}"
                                WidthRequest="40" HeightRequest="40"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ToggleFavoriteCommand}"
                                CommandParameter="{Binding .}">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding Favorito}" Value="True">
                                    <Setter Property="TextColor" Value="{StaticResource FavoriteGold}" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                    </Grid>
                    <Grid ColumnDefinitions="*,*,Auto" ColumnSpacing="8">
                        <Button Grid.Column="0" Text="Usuário" FontSize="12.5" HeightRequest="36"
                                BackgroundColor="{StaticResource SurfaceDark}" TextColor="{StaticResource TextPrimaryDark}"
                                IsVisible="{Binding HasUser}"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CopyUserCommand}"
                                CommandParameter="{Binding .}" />
                        <Button Grid.Column="1" Text="Senha" FontSize="12.5" HeightRequest="36"
                                BackgroundColor="{StaticResource SurfaceDark}" TextColor="{StaticResource TextPrimaryDark}"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CopyPasswordCommand}"
                                CommandParameter="{Binding .}" />
                        <Button Grid.Column="2" Text="&#9998;" FontSize="14" WidthRequest="38" HeightRequest="36" Padding="0"
                                BackgroundColor="{StaticResource SurfaceDark}" TextColor="{StaticResource TextSecondaryDark}"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenEditorCommand}"
                                CommandParameter="{Binding .}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="SecretBoxRowTemplate">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10" Padding="12" Margin="0,0,0,8"
                  behaviors:HoverCursor.IsHand="True">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer
                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenEditorCommand}"
                        CommandParameter="{Binding .}" />
                </Grid.GestureRecognizers>
                <Border Grid.Column="0" WidthRequest="36" HeightRequest="36" StrokeShape="RoundRectangle 11"
                        BackgroundColor="{StaticResource Tertiary}" StrokeThickness="0">
                    <Label Text="{Binding Initial}" TextColor="{StaticResource White}"
                           FontFamily="OpenSansSemibold" FontSize="14"
                           HorizontalOptions="Center" VerticalOptions="Center" />
                </Border>
                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                    <Label Text="{Binding BoxName}" FontFamily="OpenSansSemibold" FontSize="14.5"
                           TextColor="{StaticResource TextPrimaryDark}" LineBreakMode="TailTruncation" />
                    <Label Text="{Binding Url}" FontSize="12" TextColor="{StaticResource TextSecondaryDark}"
                           IsVisible="{Binding HasUrl}" LineBreakMode="TailTruncation" />
                </VerticalStackLayout>
                <Button Grid.Column="2" Text="&#9733;" FontSize="17" Padding="0"
                        BackgroundColor="Transparent" TextColor="{StaticResource TextMutedDark}"
                        WidthRequest="36" HeightRequest="36"
                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ToggleFavoriteCommand}"
                        CommandParameter="{Binding .}">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" Binding="{Binding Favorito}" Value="True">
                            <Setter Property="TextColor" Value="{StaticResource FavoriteGold}" />
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
            </Grid>
        </DataTemplate>
    </ContentPage.Resources>

    <Grid>

        <!-- Layout compacto (celular, largura < 700dp): lista cheia + editor em bottom-sheet -->
        <Grid RowDefinitions="Auto,*" IsVisible="{Binding IsCompactLayout}">

            <VerticalStackLayout Grid.Row="0" Padding="18,16,18,10" Spacing="12" BackgroundColor="{StaticResource Tertiary}">
                <!-- O nome do cofre vive no cabeçalho da página, não no Title do Shell: com os
                     dois, o layout largo mostrava o nome duas vezes. Aqui ele acompanha o
                     layout largo, que já fazia assim. -->
                <Grid ColumnDefinitions="*,Auto">
                    <Label Grid.Column="0" Text="{Binding VaultName}" FontFamily="OpenSansSemibold" FontSize="18"
                           TextColor="{StaticResource TextPrimaryDark}" LineBreakMode="TailTruncation" VerticalOptions="Center" />
                    <Button Grid.Column="1" Text="&#9881;" FontSize="18" Padding="0"
                            BackgroundColor="Transparent" TextColor="{StaticResource TextSecondaryDark}"
                            WidthRequest="38" HeightRequest="38"
                            Command="{Binding OpenVaultSettingsCommand}" />
                </Grid>
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                    <Border Grid.Column="0" BackgroundColor="{StaticResource SurfaceCardDark}" Stroke="{StaticResource BorderDarkBrush}"
                            StrokeShape="RoundRectangle 12" Padding="14,0" HeightRequest="44">
                        <Entry Placeholder="Buscar por nome..." Text="{Binding SearchText}"
                               TextColor="{StaticResource TextPrimaryDark}" PlaceholderColor="{StaticResource TextMutedDark}"
                               BackgroundColor="Transparent" VerticalOptions="Center" />
                    </Border>
                    <Button Grid.Column="1" Text="&#43;" FontSize="20" WidthRequest="44" HeightRequest="44" Padding="0"
                            Style="{StaticResource PrimaryActionButtonStyle}"
                            Command="{Binding AddNewItemCommand}" />
                </Grid>
                <HorizontalStackLayout Spacing="8">
                    <controls:SelectableChip Text="Todos" Style="{StaticResource FilterChipStyle}"
                            Command="{Binding SetFilterAllCommand}" IsSelected="{Binding IsAllFilterSelected}" />
                    <controls:SelectableChip Text="Favoritos" Style="{StaticResource FilterChipStyle}"
                            Command="{Binding SetFilterFavoritesCommand}" IsSelected="{Binding FilterFavoritesOnly}" />
                </HorizontalStackLayout>
            </VerticalStackLayout>

            <CollectionView Grid.Row="1" Margin="16,10" ItemsSource="{Binding Items}"
                             ItemTemplate="{StaticResource SecretBoxCardTemplate}" />

        </Grid>

        <!-- Overlay do editor em bottom-sheet (só no layout compacto - no layout largo o editor já
             aparece no painel lateral; sem o IsCompactLayout aqui, isso vazava por cima do painel
             lateral esticado até o fim da janela quando IsEditorOpen ficava true no modo tablet). -->
        <Grid IsVisible="{Binding IsCompactEditorOpen}" BackgroundColor="#99000000">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseEditorCommand}" />
            </Grid.GestureRecognizers>
            <Border VerticalOptions="End" BackgroundColor="{StaticResource SurfaceCardDark}"
                    StrokeShape="RoundRectangle 22,22,0,0" StrokeThickness="0" Padding="20" MaximumHeightRequest="620">
                <ScrollView>
                    <views:ItemEditorView />
                </ScrollView>
            </Border>
        </Grid>

        <!-- Layout largo (tablet, largura >= 700dp): mestre-detalhe -->
        <Grid ColumnDefinitions="360,*" IsVisible="{Binding IsWideLayout}">

            <Grid Grid.Column="0" RowDefinitions="Auto,*" BackgroundColor="{StaticResource SurfaceDark}">
                <VerticalStackLayout Grid.Row="0" Padding="18,20,18,14" Spacing="12" BackgroundColor="{StaticResource Tertiary}">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="{Binding VaultName}" FontFamily="OpenSansSemibold" FontSize="18"
                               TextColor="{StaticResource TextPrimaryDark}" VerticalOptions="Center" />
                        <Button Grid.Column="1" Text="&#9881;" FontSize="18" Padding="0"
                                BackgroundColor="Transparent" TextColor="{StaticResource TextSecondaryDark}"
                                WidthRequest="38" HeightRequest="38"
                                Command="{Binding OpenVaultSettingsCommand}" />
                    </Grid>
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                        <Border Grid.Column="0" BackgroundColor="{StaticResource SurfaceCardDark}" Stroke="{StaticResource BorderDarkBrush}"
                                StrokeShape="RoundRectangle 12" Padding="14,0" HeightRequest="42">
                            <Entry Placeholder="Buscar por nome..." Text="{Binding SearchText}"
                                   TextColor="{StaticResource TextPrimaryDark}" PlaceholderColor="{StaticResource TextMutedDark}"
                                   BackgroundColor="Transparent" VerticalOptions="Center" />
                        </Border>
                        <Button Grid.Column="1" Text="&#43;" FontSize="18" WidthRequest="42" HeightRequest="42" Padding="0"
                                Style="{StaticResource PrimaryActionButtonStyle}"
                                Command="{Binding AddNewItemCommand}" />
                    </Grid>
                    <HorizontalStackLayout Spacing="8">
                        <controls:SelectableChip Text="Todos" Style="{StaticResource FilterChipStyle}"
                                Command="{Binding SetFilterAllCommand}" IsSelected="{Binding IsAllFilterSelected}" />
                        <controls:SelectableChip Text="Favoritos" Style="{StaticResource FilterChipStyle}"
                                Command="{Binding SetFilterFavoritesCommand}" IsSelected="{Binding FilterFavoritesOnly}" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
                <CollectionView Grid.Row="1" Margin="12,10" ItemsSource="{Binding Items}"
                                 ItemTemplate="{StaticResource SecretBoxRowTemplate}" />
            </Grid>

            <BoxView Grid.Column="0" HorizontalOptions="End" WidthRequest="1"
                     Color="{StaticResource BorderDark}" />

            <Grid Grid.Column="1" Padding="44">
                <ScrollView IsVisible="{Binding ShowItemEditor}">
                    <views:ItemEditorView MaximumWidthRequest="560" HorizontalOptions="Start" />
                </ScrollView>
                <Label Text="Selecione um item na lista ao lado ou crie um novo" IsVisible="{Binding ShowEmptyState}"
                       TextColor="{StaticResource TextSecondaryDark}" FontSize="14"
                       HorizontalOptions="Center" VerticalOptions="Center" />
            </Grid>

        </Grid>

        <!-- Toast de feedback (copiar usuário/senha etc.) - controlado via ToastRequested no code-behind. -->
        <Border x:Name="ToastBorder" BackgroundColor="{StaticResource SurfaceCardDark}"
                Stroke="{StaticResource BorderDarkBrush}" StrokeShape="RoundRectangle 12"
                Padding="18,12" HorizontalOptions="Center" VerticalOptions="End" Margin="0,0,0,28"
                Opacity="0" InputTransparent="True">
            <Label x:Name="ToastLabel" TextColor="{StaticResource TextPrimaryDark}" FontSize="14"
                   FontFamily="OpenSansSemibold" />
        </Border>

        <!-- Disparado por SecretCreated / SecretUpdated / SecretDeleted, que só ocorrem quando a
             gravação no arquivo deu certo - ver SealOverlayView/LockSealDrawable. -->
        <views:SealOverlayView x:Name="SealOverlay" />

    </Grid>
</ContentPage>
